# Path to the canonical SQLite database. Defaults to ./data/pcf.db.
# PCF_DB_PATH=./data/pcf.db

# Optional later override for the Claude Code model (SPEC.md §16). Leave unset in v0.1.
# PCF_CLAUDE_MODEL=

# No ANTHROPIC_API_KEY is used by this application (SPEC.md §16, §34).
